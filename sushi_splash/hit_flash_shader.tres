[gd_resource type="Shader" format=3 uid="uid://e4bkfgumedit"]

[resource]
code = "shader_type canvas_item;

uniform float val : hint_range(0.0, 1.0) = 0;

void fragment(){
	vec4 curPixelColor = texture(TEXTURE,UV);
	curPixelColor.rgb = curPixelColor.rgb+vec3(1,1,1)*(val*2.0);

	COLOR = curPixelColor;
}"
